[module/date]
type = internal/date

format = %{A1:calendar_toggle:}  <label>%{A}

format-padding = 0
format-background = ${colors.date-bg} 
format-foreground = ${colors.date-fg}

date = %a %d %b

label = "%date%"
