[module/date]
type = internal/date

format = %{A1:calendar_toggle:} <label>%{A}

format-padding = 1
format-background = #81A1C1
format-foreground = #2E3440

date = %a %d %b

label = "%date%"

click-left = eww close calendar > /dev/null || eww open calendar
